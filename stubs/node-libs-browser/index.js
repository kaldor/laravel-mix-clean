/**
 * Stub for node-libs-browser.
 *
 * laravel-mix lists node-libs-browser as a dependency but does not import or
 * use it at runtime. This stub eliminates the transitive dependency chain that
 * leads to the vulnerable elliptic package (GHSA-848j-6mx2-7j84).
 */
module.exports = {};
